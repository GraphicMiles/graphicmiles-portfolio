# Graphic Miles — Portfolio

Personal portfolio website, served as a single static HTML file built with Vite.

## Run locally

```bash
npm install
npm run dev
```

Build and preview:

```bash
npm run build
npm run preview
```

The site is fully self-contained in `index.html` (inline CSS and JavaScript).
