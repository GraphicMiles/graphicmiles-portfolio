{
  "name": "graphic-miles-portfolio",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test api/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.3.2"
  }
}
